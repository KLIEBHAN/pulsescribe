UX notes - 2026-04-19

Latest pass - Settings UX (`ui/settings_windows.py`)
- Scope: targeted the next high-value UX area in Windows Settings without touching prompt editors.
- Reviewed weaknesses first:
  1. Providers tab only showed a bare `✓`, so it was unclear which API key was required vs optional.
  2. Setup overview/how-to copy was functional but not especially actionable or scannable.
  3. Save feedback was too thin: success/error mostly lived in the button text.
  4. Hotkey guidance in Settings lagged behind the clearer onboarding experience.
- Changes in this pass:
  - added mode-aware provider guidance text in the Providers tab
  - replaced opaque API-key checkmarks with inline states like `Required`, `Configured`, `Optional`, `Not needed`
  - improved API-key placeholders/tooltips and added basic accessibility names
  - improved Setup card copy and formatted displayed hotkeys for readability
  - improved Hotkeys guidance/tooltips and clearer status messages during recording/preset/clear flows
  - added persistent footer save status feedback, including restart guidance when daemon reload signaling fails
  - kept existing settings behavior / persistence model unchanged
- Validation:
  - `pytest tests/test_settings_windows_load.py tests/test_settings_windows_hotkeys.py tests/test_settings_windows_logs.py tests/test_settings_ui.py -q`
  - `python -m py_compile ui/settings_windows.py tests/test_settings_windows_load.py tests/test_settings_windows_hotkeys.py tests/test_settings_windows_logs.py`
  - `ruff check ui/settings_windows.py tests/test_settings_windows_load.py tests/test_settings_windows_hotkeys.py tests/test_settings_windows_logs.py`

Previous pass - Onboarding / shared setup UX
- Improved Windows onboarding guidance, hotkey capture discoverability, safe dictation test messaging, shared permission card wording, and Windows focus/placeholder styling.

Next worthwhile UX areas
1. `ui/welcome.py`
- empty states, setup overview hierarchy and action clarity
2. overlay / recording feedback
- more explicit transient processing / retry messaging
3. advanced local settings in `ui/settings_windows.py`
- optional defaults vs advanced overrides could be explained even more clearly
