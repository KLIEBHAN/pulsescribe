Performance work log - 2026-04-09 (macOS onboarding permission snapshot reuse)

Performance work log - 2026-04-09 (shared animation frame cache + Windows onboarding dedupe)

Scope
- Left the last two commit areas untouched again: `ui/onboarding_wizard.py`, `ui/permissions_card.py`, `ui/overlay.py`, `ui/overlay_windows.py`, and `ui/overlay_pyside6.py`.
- Avoided the already dirty prompt/permissions/vocabulary/settings files and their related tests.
- Focused on the shared frontend math path in `ui/animation.py` plus the Windows onboarding flow in `ui/onboarding_wizard_windows.py`.

Current weaknesses reviewed
- `ui/animation.py`: every overlay frame recomputed the same state/time-specific animation math once per bar, even though all bars in a frame share the same timestamp and most phase inputs.
- `ui/onboarding_wizard_windows.py`: repeated navigation updates revalidated unchanged hotkey pairs and rebuilt unchanged hotkey summary text.
- `ui/onboarding_wizard_windows.py`: FAST mode reapplied its preset on `Weiter` even when the user had not changed the API key since the previous apply.
- `ui/onboarding_wizard_windows.py`: IPC polling repeatedly called `setInterval()` with the interval already in effect.

Changes in this pass
- `ui/animation.py`
  Added a tiny per-frame cache so each overlay backend reuses one animation-value build per state/timestamp instead of recomputing shared math for every bar call.
- `ui/onboarding_wizard_windows.py`
  Cached hotkey validation results for unchanged toggle/hold pairs and skipped redundant test-step hotkey summary refresh work.
- `ui/onboarding_wizard_windows.py`
  Added a FAST-mode reapply flag so the wizard only reapplies the preset when new API-key input actually needs persistence.
- `ui/onboarding_wizard_windows.py`
  Deduped timer-interval changes on the IPC polling path when the target interval is already active.
- Tests
  Added regression coverage for animation frame-cache reuse, unchanged FAST-mode next-step behavior, hotkey-validation caching, and duplicate idle-interval suppression.

Why this helps
- All overlay implementations now pay the heavy animation math once per rendered frame instead of once per bar, which trims CPU work on a central high-frequency UI path without changing visuals.
- The Windows onboarding wizard does less repeated validation, less repeated text work, and fewer no-op timer updates while preserving the same flow and guardrails.
- The FAST-mode first-run path avoids unnecessary env/preset work when the user simply proceeds with already-applied settings.

Validation
- `pytest tests/test_animation.py tests/test_onboarding_windows.py -q`
- `python -m py_compile ui/animation.py ui/onboarding_wizard_windows.py tests/test_animation.py tests/test_onboarding_windows.py`
- `ruff check ui/animation.py ui/onboarding_wizard_windows.py tests/test_animation.py tests/test_onboarding_windows.py`

Next worthwhile areas
- `pulsescribe_daemon.py` + `ui/menubar.py`: the recording preview still dispatches high-frequency UI updates even when the visible menu-bar preview does not materially change.
- `ui/settings_windows.py`: the still-dirty settings surface likely has remaining no-op label/visibility churn, but it should be handled only after the current unrelated local edits are settled.
- `ui/animation.py`: if traces still show overlay cost, the next step is frame-level batch APIs in callers so each backend can consume one precomputed bar vector directly.

Scope
- Left the last two commit areas untouched: `ui/overlay.py`, `ui/overlay_windows.py`, `ui/overlay_pyside6.py`, and `ui/welcome.py`.
- Avoided the already dirty prompt/permissions/vocabulary files and their related tests.
- Focused on the remaining macOS onboarding path in `ui/onboarding_wizard.py` plus the shared permission-card cache surface in `ui/permissions_card.py`.

Current weaknesses reviewed
- `ui/onboarding_wizard.py`: every permissions-step render recomputed `can_advance` by calling OS permission helpers again, even when `PermissionsCard.refresh()` had just read the same state.
- `ui/onboarding_wizard.py`: the summary step also re-queried microphone/accessibility/input-monitoring state instead of reusing the already known permission snapshot.
- `ui/onboarding_wizard.py` + `ui/hotkey_card.py`: a successful hotkey change triggered follow-up field sync and render work twice in a row, once in the wizard callback and once again in the shared hotkey card.

Changes in this pass
- `ui/permissions_card.py`
  Exposed the most recent permission snapshot through a small accessor so host UIs can reuse the last refresh result.
- `ui/onboarding_wizard.py`
  Added a local permission-signature reader that prefers the cached card snapshot before falling back to OS permission helpers.
- `ui/onboarding_wizard.py`
  Routed both permissions-step navigation gating and summary rendering through the shared permission snapshot.
- `ui/onboarding_wizard.py`
  Removed duplicate hotkey success follow-up work from the wizard so `HotkeyCard` remains the single owner of the post-save sync/render pass.
- Tests
  Added regression coverage for permission-cache reuse and for the streamlined hotkey success path.

Why this helps
- The macOS onboarding wizard now avoids repeated permission-helper calls on a render-heavy path where the permissions card already owns fresh state.
- Hotkey changes do one post-save UI sync/render pass instead of two, which trims unnecessary widget churn on an interactive setup step.
- The changes stay small and local while keeping onboarding behavior unchanged.

Validation
- `pytest tests/test_onboarding.py tests/test_permissions_card.py -q`
- `python -m py_compile ui/onboarding_wizard.py ui/permissions_card.py tests/test_onboarding.py tests/test_permissions_card.py`
- `ruff check ui/onboarding_wizard.py ui/permissions_card.py tests/test_onboarding.py tests/test_permissions_card.py`

Next worthwhile areas
- `ui/overlay.py`: animation timers still look like the next likely high-frequency CPU hotspot on macOS if profiling shows wakeups outside interim formatting.
- `ui/menubar.py`: long dictation sessions may still benefit from stronger preview stabilization if menu-bar title updates show up in traces.
- `ui/onboarding_wizard.py`: the Fast-mode API-key prompt still forces a render after focusing; if onboarding traces still show churn there, the prompt visibility path could be narrowed further.

Performance work log - 2026-04-09 (overlay interim tail normalization fast path)

Scope
- Left the last two commit areas untouched again: `ui/welcome.py` and `ui/settings_windows.py`.
- Avoided the already dirty prompt/permissions/vocabulary files and their related tests.
- Focused on the shared overlay recording-interim formatting path in `ui/overlay.py`, `ui/overlay_windows.py`, and `ui/overlay_pyside6.py`.

Current weaknesses reviewed
- All overlay implementations normalized recording interim text with `" ".join(text.split())` on every update, even though the UI only renders a short tail preview.
- During dictation this path runs repeatedly on visible UI updates, so long whitespace-heavy interim payloads paid full-string splitting and joining cost without improving what the user sees.
- The same avoidable recomputation existed across macOS, Tk, and PySide overlay frontends.

Changes in this pass
- `ui/overlay.py`
  Reworked recording interim formatting to normalize from the tail backwards and stop once the visible preview is known.
- `ui/overlay_windows.py`
  Applied the same tail-oriented normalization so the Windows fallback overlay avoids full-text recomputation on interim updates.
- `ui/overlay_pyside6.py`
  Applied the same optimization to the PySide6 overlay path for parity with the other frontends.
- Tests
  Added regression coverage for long whitespace-heavy interim payloads across all overlay implementations.

Why this helps
- Recording overlays now scale with the short visible preview instead of the full interim text payload on each update.
- This reduces repeated allocations and string work in a high-frequency, user-visible UI path while preserving the rendered output.
- The change is local, architecture-friendly, and keeps overlay behavior consistent across frontends.

Validation
- `pytest tests/test_overlay.py tests/test_overlay_windows.py tests/test_overlay_pyside6.py -q`
- `python -m py_compile ui/overlay.py ui/overlay_windows.py ui/overlay_pyside6.py tests/test_overlay.py tests/test_overlay_windows.py tests/test_overlay_pyside6.py`
- `ruff check ui/overlay.py ui/overlay_windows.py ui/overlay_pyside6.py tests/test_overlay.py tests/test_overlay_windows.py tests/test_overlay_pyside6.py`

Next worthwhile areas
- `ui/overlay.py`: the recording/listening animation timers still execute frequent per-bar math; if traces still show CPU cost there, the next pass should short-circuit fully settled frames and avoid no-op transactions.
- `ui/menubar.py`: recording preview churn is already deduped at the title level, but long dictations may still justify stronger preview stabilization if profiling shows menu-bar wakeups.
- `ui/overlay_windows.py` and `ui/overlay_pyside6.py`: if recording sessions still show pressure after this pass, the next likely improvement is skipping label/style updates earlier in the state path before formatting work happens.

Performance work log - 2026-04-09 (macOS welcome logs incremental chunk reuse)

Scope
- Left the last two commit areas untouched again: `ui/settings_windows.py`, `ui/overlay_windows.py`, and `utils/log_tail.py`.
- Avoided the already dirty worktree files from earlier local changes (`utils/custom_prompts.py`, `utils/permissions.py`, `utils/vocabulary.py`, related tests).
- Focused on the remaining macOS frontend hotspot in `ui/welcome.py` for the Logs tab refresh path.

Current weaknesses reviewed
- `ui/welcome.py`: append-only log growth still rebuilt the visible tail string from the full cached payload on every refresh attempt.
- `ui/welcome.py`: when the log file only grew but the user was not scrolled to the bottom, the refresh path fell back to a full tail reload instead of reusing already rendered content.
- `ui/welcome.py`: log refresh state tracked only the rendered string/signature, not reusable visible chunks, so incremental updates could not cheaply rebuild a trimmed tail.

Changes in this pass
- `ui/welcome.py`
  Added a lightweight cache for visible log chunks plus truncation state.
- `ui/welcome.py`
  Extended append-only log refreshes so they can reuse cached chunks even when the view is not near the bottom, rebuilding only the visible tail instead of rereading the full log tail.
- `ui/welcome.py`
  Consolidated full log payload application into one helper that preserves scroll position while keeping the chunk cache in sync.
- Tests
  Added regression coverage that append-only log growth reuses cached chunks and skips a full reload when the viewer is scrolled away from the bottom.

Why this helps
- The macOS Logs tab now avoids repeated full-tail recomputation during long-running sessions with steady log append traffic.
- Background auto-refresh and manual refresh remain responsive because append-only updates reuse in-memory state even for users reading older log lines.
- The optimization keeps behavior intact while reducing file I/O and string rebuilding on a visible UI path.

Validation
- `pytest tests/test_settings_ui.py -q`
- `python -m py_compile ui/welcome.py tests/test_settings_ui.py`
- `ruff check ui/welcome.py tests/test_settings_ui.py`

Next worthwhile areas
- `ui/overlay.py`: the macOS overlay still updates visual state at high frequency and is the next likely CPU hotspot if traces still show wakeups outside recording.
- `ui/menubar.py`: interim preview title updates could be further short-circuited if long dictation sessions still show menu-bar churn.
- `ui/welcome.py`: prompt/vocabulary save validation remains a candidate if tab-edit latency is still noticeable after the log-viewer improvement.

Performance work log - 2026-04-09 (Windows settings transcripts incremental block reuse)

Scope
- Left the last two commit areas untouched again: `ui/onboarding_wizard_windows.py`, `ui/welcome.py`, and `utils/history.py`.
- Focused on the remaining Windows frontend hotspot in `ui/settings_windows.py` when the transcript history grows while the user is viewing older entries.
- Preserved transcript ordering, scroll-position behavior, transcript count labels, and the existing incremental append path.

Current weaknesses reviewed
- `ui/settings_windows.py`: the transcripts viewer already avoided full history rereads on append-only updates, but if the user was not scrolled to the bottom it still reformatted every visible transcript entry before replacing the document text.
- `ui/settings_windows.py`: transcript status labels were reassigned repeatedly even when the rendered count/state had not changed.

Changes in this pass
- `ui/settings_windows.py`
  Added a local cache of already formatted transcript blocks for the Windows settings viewer.
- `ui/settings_windows.py`
  Reused cached blocks plus only newly formatted entries when append-only history updates require a document replace instead of an in-place append.
- `ui/settings_windows.py`
  Switched transcript status/error/success label writes to the existing deduping label helper.
- Tests
  Added regression coverage that append-only transcript refreshes reuse cached blocks and avoid reformatting older visible entries.

Why this helps
- Long-running Windows settings sessions with an open transcript tab now avoid repeated per-entry formatting work on every append-only refresh when the user is inspecting older transcript history.
- The visible transcript view stays responsive because the heavy part of the replace path scales with only new entries instead of the whole retained window.

Validation
- `pytest tests/test_settings_windows_logs.py -q`
- `python -m py_compile ui/settings_windows.py tests/test_settings_windows_logs.py`
- `ruff check ui/settings_windows.py tests/test_settings_windows_logs.py`

Next worthwhile areas
- `ui/settings_windows.py`: the logs viewer still rebuilds merged visible text from the full tail string on each append path; a line/block cache similar to transcripts would trim that path too if profiling still shows it.
- `ui/overlay_windows.py`: interim-file polling still stats the file every 200 ms during recording; if traces still show wakeups there, the next step is reducing polling work when content is stable for longer stretches.

Performance work log - 2026-03-28

Performance work log - 2026-04-09 (macOS welcome transcripts incremental formatting pass)

Scope
- Left the last two commit areas untouched again: the recent Windows prompt/settings persistence path and unrelated local edits in `utils/custom_prompts.py`.
- Focused on the remaining macOS frontend hotspot called out in the notes: long-running transcript refreshes inside `ui/welcome.py`.
- Preserved welcome-window behavior, transcript ordering, count labels, auto-refresh flow, and scroll-position behavior.

Current weaknesses reviewed
- `ui/welcome.py`: when transcript history grew and the view was not eligible for in-place append, the refresh path reformatted the entire visible transcript history string even though the previously rendered blocks were already known in memory.
- `ui/welcome.py`: the append-aware transcript path cached raw entries, but not the already formatted welcome-view blocks, so incremental refreshes still paid avoidable per-entry formatting cost.
- `utils/history.py`: welcome transcript formatting only exposed a fully joined string API, which made block-level reuse in the UI path awkward.

Changes in this pass
- `utils/history.py`
  Added `format_transcript_entries_for_welcome()` so welcome/history rendering can reuse per-entry blocks without recomputing the entire joined payload every refresh.
- `ui/welcome.py`
  Cached the last rendered transcript blocks alongside entries/text and reused them during incremental append refreshes.
- `ui/welcome.py`
  When new transcript lines arrive but in-place UI append is not possible, the welcome window now rebuilds the visible text from cached blocks plus only the newly formatted blocks, instead of reformatting every retained transcript entry.
- Tests
  Added regression coverage for block-level formatting and for transcript refreshes that must preserve scroll position while still avoiding a full payload reload.

Why this helps
- Long-running welcome-window transcript sessions do less repeated formatting work on every append-only refresh.
- Auto-refresh keeps the transcript viewer responsive even when the user is scrolled away from the bottom and the code must replace the document text instead of appending directly.
- The optimization stays local to the render path and keeps behavior unchanged while reducing repeated computation on a visible UI path.

Validation
- `pytest tests/test_settings_ui.py tests/test_history_display.py -q`
- `python -m py_compile ui/welcome.py utils/history.py tests/test_settings_ui.py tests/test_history_display.py`
- `ruff check ui/welcome.py utils/history.py tests/test_settings_ui.py tests/test_history_display.py`

Next worthwhile areas
- `ui/welcome.py`: the logs viewer still recomputes merged visible tails on every append path; if profiling shows it, the same block-cache idea could be applied there.
- `ui/onboarding_wizard.py`: summary/status text still has a few render-adjacent recomputation paths that could be deduped further.
- `ui/permissions_card.py`: if permission polling still shows up in traces, the next step would be short-circuiting stable checks earlier before hitting OS permission helpers.

Scope
- Left the last two commit areas untouched: transcript/log incremental refresh and permission/overlay repaint work.
- Focused on `.env` persistence churn in Windows settings/local preset flows and setup overview UI churn.
- Preserved current onboarding, settings, preset, and API key behavior.

Current weaknesses reviewed
- `ui/settings_windows.py`: `Save && Apply` rewrote `.env` once per setting and once per API key, causing a long chain of small disk reads/writes in one UI action.
- `utils/presets.py`: applying a local preset performed many separate `.env` mutations even though the user triggered one atomic configuration change.
- `ui/settings_windows.py`: setup overview labels/styles and mode-dependent card visibility were reapplied even when the rendered result had not changed, causing avoidable repaints and layout work.

Changes in this pass
- `utils/preferences.py`
  Added `update_env_settings()` to batch multiple `.env` mutations into one atomic read/write pass, preserve unrelated lines, collapse duplicate updated keys, and skip unchanged rewrites.
- `ui/settings_windows.py`
  Switched the Windows settings save path to one batched `.env` update for settings and API keys, and deduped repeated setup overview/status/visibility widget updates.
- `utils/presets.py`
  Switched local preset application to the batched `.env` update path.
- Tests
  Added regression coverage for batched env writes, no-op env updates, preset batching, canonical FP16 handling in batched saves, and batched API key persistence.

Why this helps
- `Save && Apply` no longer blocks on a long series of tiny `.env` writes, so the settings window responds faster and feels less sticky.
- Local preset application now lands as one atomic config update instead of many serial writes.
- Setup overview refreshes and mode switches avoid redundant text/style/visibility churn, which trims repaint/layout pressure in a frequently hit UI path.

Validation
- `pytest tests/test_preferences_cache.py tests/test_presets.py tests/test_settings_windows_load.py tests/test_settings_windows_hotkeys.py tests/test_settings_windows_logs.py tests/test_onboarding_windows.py -q`
- `python -m py_compile utils/preferences.py utils/presets.py ui/settings_windows.py tests/test_preferences_cache.py tests/test_presets.py tests/test_settings_windows_load.py`
- `ruff check utils/preferences.py utils/presets.py ui/settings_windows.py tests/test_preferences_cache.py tests/test_presets.py tests/test_settings_windows_load.py`

Next worthwhile areas
- `ui/onboarding_wizard_windows.py`: hotkey/test-summary paths still reread env-backed values more often than needed; a small local state cache would trim more UI churn.
- `ui/welcome.py`: the save/apply path still performs many individual env writes, but it was intentionally skipped here because welcome/log work was part of the recent commit history.
- `utils/custom_prompts.py` and vocabulary-driven tab loads: if profiling still shows tab-open spikes, add signature-based caches similar to the history/log optimizations.

Performance work log - 2026-03-28 (Onboarding pass)

Scope
- Kept the last two commit areas untouched again: settings/env batching setup work and permission/overlay repaint work.
- Focused on `ui/onboarding_wizard_windows.py`, which was the clearest remaining frontend hotspot outside those commit boundaries.
- Preserved onboarding flow, hotkey behavior, summary output, and daemon settings reload semantics.

Current weaknesses reviewed
- `ui/onboarding_wizard_windows.py`: the wizard repeatedly reread env-backed values across choose-goal, hotkey, test, summary, and navigation paths even though the same values were already known within the current dialog session.
- `ui/onboarding_wizard_windows.py`: fast/private onboarding could trigger redundant persistence work, including duplicate Deepgram key handling on Next and an extra `PULSESCRIBE_MODE=local` write before local preset application.
- `ui/onboarding_wizard_windows.py`: hotkey defaults, hotkey saves, preset application, and clears performed separate per-key env mutations and always emitted `settings_changed`, even when nothing actually changed.

Changes in this pass
- `ui/onboarding_wizard_windows.py`
  Added a lightweight wizard-local env cache with explicit refresh on step changes, so hotkey/mode/language/API-key reads no longer keep walking the env helper path on every small UI update.
- `ui/onboarding_wizard_windows.py`
  Added a single batched env update helper for the wizard and routed hotkey defaults, hotkey edits, preset application, language changes, and fast-mode mode selection through it.
- `ui/onboarding_wizard_windows.py`
  Removed duplicate fast/private writes: Next now relies on the preset application path for Deepgram key persistence, and private mode no longer writes `PULSESCRIBE_MODE=local` twice before the local preset helper runs.
- `ui/onboarding_wizard_windows.py`
  Suppressed no-op `settings_changed` emissions when language, hotkeys, API key, or mode were already current, which avoids unnecessary daemon reload work from the onboarding UI.
- Tests
  Updated Windows onboarding tests for the batched persistence path and added coverage for no-op language changes and duplicate fast-mode persistence avoidance.

Why this helps
- The Windows onboarding dialog now does materially less env I/O while the user configures hotkeys, mode, and language.
- Hotkey interactions persist as one atomic pair update instead of multiple independent writes, which shortens the critical UI path and keeps the state transition consistent.
- Avoiding no-op `settings_changed` emissions removes unnecessary downstream reload work, improving responsiveness especially when users revisit the same selection or preset.

Validation
- `pytest tests/test_onboarding_windows.py -q`
- `python -m py_compile ui/onboarding_wizard_windows.py tests/test_onboarding_windows.py`
- `ruff check ui/onboarding_wizard_windows.py tests/test_onboarding_windows.py`

Next worthwhile areas
- `ui/onboarding_wizard.py` and `ui/hotkey_card.py`: the macOS onboarding flow still re-queries env-backed hotkey state during render cycles; a similar local-state pass would keep cross-platform behavior aligned.
- `ui/welcome.py`: the welcome save/apply flow still has a large batched-settings opportunity, but it remains adjacent to recent commit history and should be handled in a separate pass.
- Prompt/vocabulary editor tabs remain the next likely source of open-tab latency if profiling still shows spikes after the onboarding cleanup.

Performance work log - 2026-04-09 (macOS welcome + onboarding pass)

Scope
- Kept the last two commits untouched again: Windows settings batching and Windows onboarding env churn.
- Focused on the next macOS/frontend hotspots instead: `ui/welcome.py` and `ui/onboarding_wizard.py`.
- Preserved onboarding flow, welcome-window behavior, prompt/vocabulary editing, and settings persistence semantics.

Current weaknesses reviewed
- `ui/welcome.py`: `Save & Apply` still wrote `.env` one key at a time, so one UI action could trigger a long serial chain of reads/writes.
- `ui/welcome.py`: prompt/vocabulary tabs reloaded backing data more often than needed during a single window session.
- `ui/welcome.py`: mode-dependent hide/show updates still hit AppKit views even when the visibility state was unchanged.
- `ui/onboarding_wizard.py`: render-adjacent paths (`_can_advance`, summary, hotkey preview, fast-mode selection) repeatedly reread env-backed values inside the same wizard session.

Changes in this pass
- `ui/welcome.py`
  Switched the macOS `Save & Apply` path to a single batched `.env` update, including API keys, provider settings, local overrides, refine/display toggles, and legacy key cleanup.
- `ui/welcome.py`
  Added lightweight session caches for loaded prompts/defaults and vocabulary keywords so tab-open and save paths stop reloading the same data repeatedly within one window session.
- `ui/welcome.py`
  Deduped mode-dependent visibility changes so AppKit hide/show work is skipped when the effective state is already current.
- `ui/onboarding_wizard.py`
  Added a wizard-local env cache and routed fast-mode persistence, language persistence, hotkey validation/save state, summary rendering, and test-hotkey preview reads through it.
- Tests
  Updated welcome/onboarding tests for batched env persistence, session caching, and no-op visibility churn suppression.

Why this helps
- The macOS welcome window now persists settings in one atomic `.env` write instead of many small writes, which shortens the critical save path and reduces UI stickiness.
- Prompt/vocabulary tabs reuse already loaded data during the same session, so opening or saving those heavier editors avoids redundant disk work and recomputation.
- The macOS onboarding wizard no longer re-reads the same env values on each small render/update step, which trims unnecessary work in a render-sensitive first-run flow.
- Skipping redundant AppKit `setHidden_` churn reduces needless UI work on frequent mode/backend toggles.

Validation
- `pytest tests/test_settings_ui.py tests/test_onboarding.py tests/test_onboarding_wizard_flow.py -q`
- `pytest tests/test_custom_prompts.py tests/test_vocabulary.py -q`
- `python -m py_compile ui/welcome.py ui/onboarding_wizard.py tests/test_settings_ui.py tests/test_onboarding.py tests/test_onboarding_wizard_flow.py`
- `ruff check ui/welcome.py ui/onboarding_wizard.py tests/test_settings_ui.py tests/test_onboarding.py tests/test_onboarding_wizard_flow.py`

Next worthwhile areas
- `ui/hotkey_card.py`: still reads env-backed hotkey state directly; a tiny shared cache layer would align it with the wizard optimizations.
- `ui/welcome.py` logs/transcripts view switching could be profiled for further paint/layout trimming if the window is kept open for long sessions.
- If tab-open latency remains noticeable, the next likely area is moving prompt/vocabulary validation work off the immediate interaction path.

Performance work log - 2026-04-09 (hotkey card follow-up)

Scope
- Built on the current macOS welcome/onboarding state without touching the last two Windows-focused commits.
- Focused only on the shared hotkey UI path in `ui/hotkey_card.py` and its macOS callers in `ui/welcome.py` and `ui/onboarding_wizard.py`.
- Preserved hotkey validation, persistence semantics, recorder behavior, and visible UI flow.

Current weaknesses reviewed
- `ui/hotkey_card.py`: after successful hotkey changes, the card still reread hotkeys through the env/preferences path even when the parent controller already had current values cached.
- `ui/welcome.py`: the welcome window cached env settings broadly, but hotkey saves did not feed that cache back into the shared hotkey card.
- `ui/onboarding_wizard.py`: the wizard already tracked cached hotkey state, but the shared hotkey card still fell back to fresh env reads instead of reusing it.

Changes in this pass
- `ui/hotkey_card.py`
  Added an optional current-hotkeys provider so the shared card can sync from caller-owned cached state instead of always reading env-backed preferences.
- `ui/welcome.py`
  Wired the hotkey card to the welcome-window env cache and refreshed that cache immediately after successful hotkey saves.
- `ui/onboarding_wizard.py`
  Wired the hotkey card to the wizard-local hotkey cache so repeat syncs during onboarding stay in-memory.
- Tests
  Added regression coverage that the shared hotkey card prefers the cached provider path over direct env reads.

Why this helps
- Hotkey preset clicks, recorded hotkey saves, and follow-up field syncs now avoid redundant env lookups in the main macOS UI flows.
- The shared hotkey component no longer forces an extra read path immediately after the controller has already updated its own hotkey state.
- This trims repeated work on a user-visible interaction path that tends to be exercised several times while configuring shortcuts.

Validation
- `pytest tests/test_hotkey_card.py tests/test_settings_ui.py tests/test_onboarding.py tests/test_onboarding_wizard_flow.py -q`
- `python -m py_compile ui/hotkey_card.py ui/welcome.py ui/onboarding_wizard.py tests/test_hotkey_card.py tests/test_settings_ui.py`
- `ruff check ui/hotkey_card.py ui/welcome.py ui/onboarding_wizard.py tests/test_hotkey_card.py tests/test_settings_ui.py`

Next worthwhile areas
- `ui/welcome.py` logs/transcripts view switching remains the next likely long-session UI hotspot.
- `ui/onboarding_wizard.py` still has some render-adjacent status and summary recomputation that could be deduped further if profiling still shows churn.
- If prompt/vocabulary tabs still feel heavy, move non-essential validation or formatting work off the immediate interaction path.

Performance work plan - 2026-04-09 (settings vocabulary pass)

Scope
- Leave the last two commit areas untouched: `ui/hotkey_card.py`, `ui/welcome.py`, `ui/onboarding_wizard.py`, and their immediate test-only follow-up.
- Avoid `utils/custom_prompts.py` because the worktree already contains unrelated local edits there.
- Focus on the remaining Windows frontend hotspot in the settings vocabulary tab.

Current weaknesses reviewed
- `ui/settings_windows.py`: opening or saving the vocabulary tab still triggers duplicate vocabulary file work: `load_vocabulary()` reads/parses the JSON, then `validate_vocabulary()` rereads/reparses the same file immediately afterward.
- `utils/vocabulary.py`: saving unchanged keywords still rewrites the JSON file, causing unnecessary disk I/O and cache invalidation on a user-visible save path.
- `ui/settings_windows.py`: vocabulary status labels are reassigned even when the text/style is already current, creating avoidable UI churn on tab revisits and repeated saves.

Planned changes
- Share parsed vocabulary results and validation issues through one cache-backed path in `utils/vocabulary.py`.
- Skip no-op vocabulary writes when the normalized persisted data is unchanged.
- Dedupe vocabulary status label updates in `ui/settings_windows.py`.
- Add regression coverage for cached validation reuse and no-op vocabulary saves.

Changes in this pass
- `ui/overlay_windows.py`
  Reduced queue-poller wakeups while the overlay is active but the UI queue is empty by backing the idle-active poll interval off from 16 ms to 33 ms, while keeping the fast interval for real message bursts/backlog.
- `utils/vocabulary.py`
  Unified vocabulary parsing and validation behind one cache-backed state path so load + validate no longer reread/reparse the same JSON file on each tab open/save.
- `utils/vocabulary.py`
  Skipped no-op vocabulary rewrites when the normalized persisted payload is already current, while still preserving extra JSON fields and refreshing the cache.
- `ui/settings_windows.py`
  Deduped vocabulary status label text/style updates so repeated tab revisits and saves avoid redundant widget churn.
- Tests
  Added coverage for active-overlay queue backoff, fast follow-up polling after real queue work, cached vocabulary validation reuse, and no-op vocabulary saves.

Why this helps
- The Windows overlay now wakes the UI thread less aggressively during long recording/transcribing stretches when queue traffic is sparse, which reduces avoidable scheduling churn in a hot visual path.
- Opening or saving the vocabulary tab no longer performs duplicate UTF-8 reads and JSON parsing for the same file state.
- Repeated vocabulary saves that do not change the effective keyword payload stop rewriting the file, reducing disk I/O and unnecessary cache invalidation.

Validation
- `pytest tests/test_overlay_windows.py tests/test_vocabulary.py tests/test_settings_windows_load.py -q`
- `python -m py_compile ui/overlay_windows.py ui/settings_windows.py utils/vocabulary.py tests/test_overlay_windows.py tests/test_vocabulary.py tests/test_settings_windows_load.py`
- `ruff check ui/overlay_windows.py ui/settings_windows.py utils/vocabulary.py tests/test_overlay_windows.py tests/test_vocabulary.py tests/test_settings_windows_load.py`

Next worthwhile areas
- `ui/overlay_windows.py`: interim text still has a second file-poll path during recording; if runtime profiling still shows overlay churn, consider reducing or suspending file polling when direct interim queue updates are flowing.
- `ui/settings_windows.py`: the large `_load_settings()` / `_apply_local_preset()` field population path still has room for broader signal-blocking/change-guard cleanup.
- `ui/permissions_card.py`: the auto-refresh loop still repeats stable permission checks for up to 30 seconds even after the UI state has converged.

Performance work log - 2026-04-09 (permissions + PySide6 overlay pass)

Scope
- Kept the last two commit areas untouched again: `ui/settings_windows.py`, `utils/vocabulary.py`, `ui/overlay_windows.py`, `ui/hotkey_card.py`, `ui/welcome.py`, and `ui/onboarding_wizard.py`.
- Focused on two remaining UI hotspots outside that boundary: macOS permissions auto-refresh and PySide6 overlay interim polling.
- Preserved permission flows, overlay behavior, and visible status transitions.

Current weaknesses reviewed
- `ui/permissions_card.py`: after opening System Settings, the card used a fixed 1-second repeating timer for up to 30 polls, even when the permission state stayed unchanged, creating unnecessary permission checks and UI wakeups in welcome/onboarding flows.
- `ui/overlay_pyside6.py`: during recording, the overlay continued polling the interim text file at the fast cadence even while direct interim updates were already arriving, duplicating work on a hot visible path.

Changes in this pass
- `ui/permissions_card.py`
  Replaced the fixed repeating permission refresh timer with adaptive one-shot polling that backs off from fast to slower intervals and resets to the fast cadence when a permission state actually changes.
- `ui/overlay_pyside6.py`
  Added a short grace window after direct interim updates and slowed interim-file polling during that window, then restored the fast polling interval once direct updates stop.
- Tests
  Added regression coverage for permission refresh backoff/reset behavior and for suspending/restoring PySide6 interim file polling around direct interim updates.

Why this helps
- The shared permissions card now performs materially fewer redundant system permission checks while the UI waits for the user to react in System Settings.
- The PySide6 overlay no longer keeps hammering the interim text file while fresher direct updates are already driving the visible text, which cuts avoidable timer wakeups and file I/O during recording.

Validation
- `pytest tests/test_permissions_card.py tests/test_overlay_pyside6.py -q`
- `pytest tests/test_onboarding.py tests/test_settings_ui.py -q`
- `python -m py_compile ui/permissions_card.py ui/overlay_pyside6.py tests/test_permissions_card.py tests/test_overlay_pyside6.py`
- `ruff check ui/permissions_card.py ui/overlay_pyside6.py tests/test_permissions_card.py tests/test_overlay_pyside6.py`

Next worthwhile areas
- `ui/settings_windows.py`: the large settings load/apply path still looks like the next Windows UI hotspot once it is safely outside the recent-commit boundary.
- `ui/onboarding_wizard_windows.py`: the IPC test flow still polls on a fixed cadence and may benefit from similar adaptive timing if profiling shows idle wait churn.
- `ui/permissions_card.py`: if permission checks still show up in traces, add a tiny short-lived shared snapshot cache across the immediate welcome/onboarding refresh burst.

Performance work log - 2026-04-09 (onboarding render + IPC cadence pass)

Scope
- Left the last two commit areas untouched: `ui/overlay_pyside6.py`, `ui/overlay_windows.py`, `ui/permissions_card.py`, `ui/settings_windows.py`, and `utils/vocabulary.py`.
- Avoided `utils/custom_prompts.py` because the worktree already contains unrelated local edits there.
- Focused on the remaining onboarding-facing frontend hotspots in `ui/onboarding_wizard.py` and `ui/onboarding_wizard_windows.py`.

Current weaknesses reviewed
- `ui/onboarding_wizard.py`: the macOS goal step still performed a direct env lookup for language selection during UI build even though the wizard already carried an in-memory env cache.
- `ui/onboarding_wizard.py`: summary labels and Fast-mode API-key prompt widgets were still being reassigned on repeat renders even when the visible text/color/visibility was unchanged.
- `ui/onboarding_wizard_windows.py`: the Windows test-dictation IPC loop kept polling at the fast 200 ms cadence throughout a live recording session, even while waiting in an effectively idle state for the user to finish speaking.

Changes in this pass
- `ui/onboarding_wizard.py`
  Routed the language selector initialization through the wizard-local env cache instead of a fresh env helper read.
- `ui/onboarding_wizard.py`
  Added no-op guards for summary provider/hotkey/permission labels and for the Fast-mode API-key prompt visibility/message path so repeated renders stop mutating identical AppKit widgets.
- `ui/onboarding_wizard_windows.py`
  Added a small adaptive IPC polling cadence for the test-dictation flow: stay fast while connecting, slow to a gentler interval during steady-state recording, and return to the fast cadence immediately after the user requests stop.
- Tests
  Added regression coverage for duplicate summary/API-key prompt updates on macOS and for the Windows onboarding IPC polling cadence transitions.

Why this helps
- The macOS onboarding wizard now does less Objective-C/AppKit widget churn on repeated step renders, especially in the summary and Fast-mode validation paths.
- The Windows onboarding test step wakes the UI loop less aggressively while the user is actively dictating, without delaying connect or stop/result responsiveness.
- Both changes reduce frontend no-op work on first-run/setup flows while preserving existing behavior.

Validation
- `pytest tests/test_onboarding.py tests/test_onboarding_windows.py -q`
- `python -m py_compile ui/onboarding_wizard.py ui/onboarding_wizard_windows.py tests/test_onboarding.py tests/test_onboarding_windows.py`
- `ruff check ui/onboarding_wizard.py ui/onboarding_wizard_windows.py tests/test_onboarding.py tests/test_onboarding_windows.py`

Next worthwhile areas
- `ui/welcome.py`: logs/transcripts auto-refresh is still the next likely long-session frontend hotspot outside the most recent commit pair.
- `ui/onboarding_wizard_windows.py`: if profiling still shows test-step wait churn, the next step would be to reduce redundant IPC file reads in `utils/ipc.py` rather than only adjusting the UI-side timer cadence.
- `ui/overlay.py`: the macOS overlay animation timers remain a likely CPU hotspot during long active sessions once they are outside the recent-change boundary.

Performance work log - 2026-04-09 (web header + metadata pass)

Scope
- Left the last two root-repo commit areas untouched again: the recent Windows/macOS desktop UI performance work stayed out of scope.
- Focused on the untouched web frontend hotspot in `web/src/components/SiteHeader.tsx` plus adjacent metadata loading in `web/src/app/[locale]/layout.tsx`.
- Preserved mobile navigation behavior, locale switching, focus trapping, and page metadata semantics.

Current weaknesses reviewed
- `web/src/components/SiteHeader.tsx`: the header forced a full remount on every locale/path change via a changing `key`, recreating menu state, focus management, media-query listeners, body scroll locking, and `main/footer` accessibility toggles on each navigation.
- `web/src/components/SiteHeader.tsx`: the mobile focus trap re-queried focusable DOM nodes on every `Tab` keypress inside the menu, adding unnecessary DOM work in a hot interaction path.
- `web/src/app/[locale]/layout.tsx`: locale metadata generation loaded the full message object tree even though it only needed the `metadata.landing` namespace.

Changes in this pass
- `web/src/components/SiteHeader.tsx`
  Removed the forced keyed remount and replaced it with a route-scoped open state so route/locale changes close the mobile menu without rebuilding the whole header component tree.
- `web/src/components/SiteHeader.tsx`
  Cached the menu's focusable elements for the lifetime of an open session and reused the cached list inside the focus trap instead of re-running DOM queries on each `Tab`.
- `web/src/components/SiteHeader.tsx`
  Reused the discovered `main/footer` background landmark list across menu openings so the open/close path does less repeated DOM discovery work.
- `web/src/app/[locale]/layout.tsx`
  Switched metadata generation from `getMessages()` to namespace-scoped `getTranslations({ locale, namespace: "metadata.landing" })`.
- Tests
  Added coverage that the mobile menu closes correctly on route changes without depending on a component remount.

Why this helps
- Header interactions and locale/page navigations now avoid a full client-side header teardown/rebuild, which reduces no-op listener churn and DOM mutation work in the most complex client component on the site.
- The mobile menu focus trap now does its expensive DOM scan once per open instead of once per `Tab`, which trims interaction cost on slower devices.
- Locale layout metadata generation now reads only the translation namespace it needs, reducing server-side message work for page loads/builds.

Validation
- `npm run test:run -- SiteHeader HashAnchorScroller LanguageSwitcher` (in `web/`)
- `npm run lint -- 'src/components/SiteHeader.tsx' 'src/components/SiteHeader.test.tsx' 'src/app/[locale]/layout.tsx'` (in `web/`)
- `npm run build` (in `web/`)

Next worthwhile areas
- `web/src/components/HashAnchorScroller.tsx`: the 45-frame retry loop is still the next clear client-side interaction hotspot if hash-navigation traces show idle frame churn.
- `web/src/app/[locale]/page.tsx`: the landing page still performs several large `t.raw(...)` normalizations in one render pass and is the next candidate if server render time matters.
- `web/src/app/[locale]/docs/page.tsx`: the docs page has the heaviest static content derivation footprint and would benefit from the same normalization pass after the smaller header hotpath win.

Performance work log - 2026-04-09 (Windows settings load + preset pass)

Scope
- Left the last two desktop-UI commit areas untouched: `ui/onboarding_wizard.py`, `ui/onboarding_wizard_windows.py`, and the note-only web follow-up commit stayed out of scope.
- Focused on the next remaining frontend hotspot from the notes backlog: the bulk field population path in `ui/settings_windows.py`.
- Preserved settings semantics, setup overview behavior, preset behavior, and API-key persistence.

Current weaknesses reviewed
- `ui/settings_windows.py`: `_load_settings()` rewrote many combo boxes, line edits, checkboxes, sliders, and API-key status labels even when the UI already matched the loaded env snapshot.
- `ui/settings_windows.py`: bulk settings load and local preset application could still emit avoidable widget signals and mode-change side effects while populating fields.
- `ui/settings_windows.py`: `_apply_local_preset()` always refreshed setup overview and rewrote the full preset field set, even when the selected preset was already reflected in the form.
- `ui/settings_windows.py`: `_save_settings()` reread API-key fields after persistence only to derive the same status UI it had already computed.

Changes in this pass
- `ui/settings_windows.py`
  Added small widget diff helpers for checkboxes, combo boxes, and sliders, plus a temporary signal-blocking context for bulk field population.
- `ui/settings_windows.py`
  Routed `_load_settings()` through the new diff-only helpers so unchanged widget state is left alone, and blocked cascaded signals during the bulk load before applying one final mode-driven visibility update.
- `ui/settings_windows.py`
  Made `_apply_local_preset()` idempotent: repeated clicks on an already-applied preset now skip redundant field writes and skip setup-overview refresh when nothing visible changed.
- `ui/settings_windows.py`
  Reused the already-normalized API-key presence map in `_save_settings()` so status labels no longer require a second field-text pass after saving.
- Tests
  Added regression coverage that no-op settings loads avoid widget churn and that idempotent preset application avoids redundant refresh/mutation work.

Why this helps
- Opening the Windows settings window or reloading its form does less UI work, which reduces unnecessary repainting and signal/slot fan-out on a hot setup path.
- Reapplying the same local preset no longer replays the whole form population sequence, so the UI feels more immediate and avoids unnecessary setup-status recomputation.
- Saving settings keeps API-key status updates on the already-computed fast path instead of reparsing the same fields again.

Validation
- `pytest tests/test_settings_windows_load.py tests/test_settings_windows_hotkeys.py tests/test_settings_windows_logs.py -q`
- `python -m py_compile ui/settings_windows.py tests/test_settings_windows_load.py`
- `ruff check ui/settings_windows.py tests/test_settings_windows_load.py`

Next worthwhile areas
- `ui/welcome.py`: logs/transcripts auto-refresh and segment switching still look like the next macOS/frontend hotspot outside the most recent commit boundaries.
- `ui/settings_windows.py`: if traces still show expensive open-time work, the next pass should target prompt/vocabulary/log tab initialization and timer backoff rather than more field-level diffs.
- `ui/overlay.py`: the macOS overlay animation path remains a likely long-session CPU hotspot once it is the oldest untouched area again.

Performance work log - 2026-04-09 (macOS welcome logs + cached settings pass)

Scope
- Left the last two commit areas untouched: the recent `ui/settings_windows.py` load/preset optimization and the note-only follow-up commit stayed out of scope.
- Avoided `utils/custom_prompts.py` again because the worktree still contains unrelated local edits there.
- Focused on the remaining macOS/frontend hotspot in `ui/welcome.py`, especially the Logs/Transcripts tab and repeated settings initialization reads.

Current weaknesses reviewed
- `ui/welcome.py`: the Logs/Transcripts segmented control always replayed hide/show work and refresh logic even when the user reselected the already-active segment.
- `ui/welcome.py`: the logs view skipped the explicit scroll-to-bottom request when the log signature was unchanged, which kept re-opened log views from reusing the cheap cached path for the expected UI result.
- `ui/welcome.py`: the transcript count label was reassigned on every refresh even when the count text had not changed.
- `ui/welcome.py`: heavy welcome tab builders still called `get_env_setting(...)` repeatedly even though the controller already maintains an env snapshot cache for the current window session.

Changes in this pass
- `ui/welcome.py`
  Added a small `stringValue` diff helper and used it for transcript count/status updates so unchanged text widgets are left alone.
- `ui/welcome.py`
  Tracked the active Logs/Transcripts segment and skipped no-op segment switches, while routing container visibility changes through the existing hidden-state guard.
- `ui/welcome.py`
  Honored `scroll_to_bottom=True` on the unchanged-log-signature fast path so the logs panel can satisfy repeat-open behavior without rereading the file.
- `ui/welcome.py`
  Routed the welcome setup/provider/advanced/refine tab initialization reads through the controller's existing env cache instead of repeated helper lookups.
- Tests
  Added regression coverage for no-op segment reselection, transcript count-label deduping, and cached log scrolling without a reload.

Why this helps
- Re-clicking the active Logs/Transcripts segment no longer triggers redundant container mutations and refresh work on a visible tab with potentially large text content.
- The logs panel can now reuse the cached-signature fast path while still jumping to the latest tail position, which improves perceived responsiveness on repeated openings.
- Repeated transcript refreshes do less widget churn, and heavy welcome tab construction now reuses the already-loaded env snapshot more consistently.

Validation
- `pytest tests/test_settings_ui.py -q`
- `python -m py_compile ui/welcome.py tests/test_settings_ui.py`
- `ruff check ui/welcome.py tests/test_settings_ui.py`

Next worthwhile areas
- `ui/overlay.py`: the macOS overlay animation timers remain the clearest long-session CPU hotspot outside the current commit boundary.
- `ui/welcome.py`: prompt/vocabulary editors still do non-trivial text/session work on save and would benefit from a similar no-op/status-dedupe pass once the dirty `utils/custom_prompts.py` worktree is resolved.
- `ui/menubar.py`: the recording-title updates are already cheap, but the diagnostics/export actions are the next macOS surface to profile if menu interaction still feels sticky.

Performance work log - 2026-04-09 (web i18n payload + hash navigation pass)

Scope
- Left the recent desktop-app commit areas untouched (`ui/welcome.py`, `ui/settings_windows.py`, and adjacent Python-side follow-ups).
- Focused on the isolated web frontend under `web/`, where the next clear user-visible hotspots were layout-level i18n payload and hash-navigation retry work.
- Preserved locale switching, localized navigation, mobile menu behavior, SEO metadata, and static generation.

Current weaknesses reviewed
- `web/src/app/[locale]/layout.tsx`: the layout loaded full locale messages into `NextIntlClientProvider` even though the client only needed locale context plus a small set of already-known header/footer strings.
- `web/src/components/SiteHeader.tsx` and `web/src/components/LanguageSwitcher.tsx`: interactive header controls pulled translation/locale state through client hooks instead of receiving the already-resolved server values, increasing client work and hydration payload.
- `web/src/components/HashAnchorScroller.tsx`: late-anchor recovery used a per-frame retry loop, which kept waking the client during route transitions until the target appeared or retries were exhausted.

Changes in this pass
- `web/src/app/[locale]/layout.tsx`
  Reduced the client provider to locale context only and moved header/footer/common strings onto the server-resolved prop path, removing full message payload delivery from the layout.
- `web/src/components/SiteHeader.tsx`
  Switched the header to plain label props for navigation/common text so the client path no longer depends on translation hooks.
- `web/src/components/LanguageSwitcher.tsx`
  Removed translation/locale hook reads, normalized localized-path calculation locally, and kept locale transitions on the lighter client-router path when no hash is involved.
- `web/src/components/SiteFooter.tsx`
  Switched the footer to plain server-provided labels instead of client translation lookups.
- `web/src/components/HashAnchorScroller.tsx`
  Replaced the frame-by-frame retry loop with a short timeout-plus-mutation-observer path so late anchors still resolve without repeated per-frame work.
- Tests
  Updated component tests for the prop-driven i18n path and added coverage that late-anchor retry no longer relies on `requestAnimationFrame`.

Why this helps
- The web layout now ships materially less locale data to the client on every page, reducing hydration payload and translation-related client work.
- Header/footer rendering keeps more work on the server and turns the interactive client layer into a narrower state/interaction shell.
- Hash deep links still recover for streamed or delayed content, but the browser no longer burns a frame-based retry loop while waiting.

Validation
- `cd web && npm run test:run -- src/components/LanguageSwitcher.test.tsx src/components/SiteHeader.test.tsx src/components/SiteFooter.test.tsx src/components/HashAnchorScroller.test.tsx`
- `cd web && npm run lint`
- `cd web && npm run build`

Next worthwhile areas
- `web/src/app/[locale]/page.tsx` and `web/src/app/[locale]/docs/page.tsx`: large `t.raw(...)` sections could be profiled for opportunities to hoist more static data shaping out of render.
- `web/src/components/SiteHeader.tsx`: if the mobile menu still dominates interaction traces, split the always-visible shell from the open-menu dialog path to narrow the hydrated code further.
- `ui/overlay.py`: outside the web repo, the macOS overlay animation timers remain the next desktop-side long-session hotspot.

Performance work log - 2026-04-09 (web SSR translation pass)

Scope
- Left the last two commit areas untouched again: the recent `ui/welcome.py` desktop pass and the note-only follow-up stayed out of scope.
- Focused on untouched web render-path cost in `web/src/app/[locale]/page.tsx`, `web/src/app/[locale]/docs/page.tsx`, and a tiny repeat-allocation path in `web/src/components/LanguageSwitcher.tsx`.
- Preserved landing-page content, docs content, locale switching behavior, and existing routing semantics.

Current weaknesses reviewed
- `web/src/app/[locale]/page.tsx`: the landing page fetched `getTranslations("landing")` and `getTranslations("common")` sequentially even though both are independent.
- `web/src/app/[locale]/docs/page.tsx`: the docs page had the same serial translation-fetch pattern on a heavier server-render path.
- `web/src/components/LanguageSwitcher.tsx`: locale label metadata was recreated on every render even though it is static.

Changes in this pass
- `web/src/app/[locale]/page.tsx`
  Switched the landing page translation fetches to `Promise.all(...)` so the render path stops waiting on two independent async steps in series.
- `web/src/app/[locale]/docs/page.tsx`
  Switched the docs page translation fetches to the same parallel path, trimming server-side setup time on the heaviest content page.
- `web/src/components/LanguageSwitcher.tsx`
  Hoisted the static locale-name map out of the component so renders stop reallocating the same object.
- Tests
  Added a docs-page regression test file covering render success, primary call-to-action presence, and breadcrumb JSON-LD output.

Why this helps
- The landing page and docs page now start rendering as soon as both translation namespaces resolve, instead of paying avoidable serialized latency.
- The docs page benefits most because it is the larger static page and spends more total time in server-side content assembly.
- The language switcher client path is slightly cheaper on repeated renders and keeps the component work closer to pure input -> output.

Validation
- `cd web && npm run test:run -- 'src/app/[locale]/page.test.tsx' 'src/app/[locale]/docs/page.test.tsx' 'src/components/LanguageSwitcher.test.tsx'`
- `cd web && npm run lint -- 'src/app/[locale]/page.tsx' 'src/app/[locale]/docs/page.tsx' 'src/app/[locale]/docs/page.test.tsx' 'src/components/LanguageSwitcher.tsx' 'src/components/LanguageSwitcher.test.tsx'`
- `cd web && npm run build`

Next worthwhile areas
- `web/src/i18n/request.ts`: the request config still loads the full locale JSON for each request; splitting message payloads by namespace would reduce server-side i18n work further.
- `web/src/app/[locale]/page.tsx` and `web/src/app/[locale]/docs/page.tsx`: if traces still show render cost, hoist more static content shaping out of the page bodies instead of repeatedly normalizing `t.raw(...)` payloads inline.
- `web/src/components/SiteHeader.tsx`: if mobile-menu traces still dominate, split the always-visible shell from the dialog/open-menu path to shrink hydrated interaction work.

Performance work log - 2026-04-09 (macOS overlay adaptive recording cadence pass)

Scope
- Left the recent web and desktop settings/onboarding work untouched.
- Focused on `ui/overlay.py`, which remained the clearest older long-session frontend hotspot from the notes backlog.
- Preserved overlay visuals, recording responsiveness, state transitions, and feedback timing semantics.

Current weaknesses reviewed
- `ui/overlay.py`: once direct audio-level rendering started, the recording overlay kept its 60 FPS NSTimer cadence even through near-silent stretches, which meant unnecessary main-thread wakeups during long dictation sessions.
- `ui/overlay.py`: the timer cadence had no hysteresis, so a naive slowdown risked oscillating between intervals around the silence/speech boundary.

Changes in this pass
- `ui/overlay.py`
  Added an adaptive recording cadence with hysteresis: keep the fast 60 FPS path while speech energy is meaningfully active, and back off to a lower idle cadence during quiet stretches without changing the visible state model.
- `tests/test_overlay.py`
  Added regression coverage for quiet-start idle cadence and for rescheduling the recording timer when the overlay transitions from idle audio activity back to active speech.

Why this helps
- Long recording sessions now spend materially less time waking the main thread when the user pauses between phrases, while louder/active speech still returns to the high-frequency path quickly.
- The hysteresis keeps the timer from flapping between cadences around the silence threshold, so the optimization stays stable instead of trading CPU churn for timer churn.

Validation
- `pytest tests/test_overlay.py -q`
- `python -m py_compile ui/overlay.py tests/test_overlay.py`
- `ruff check ui/overlay.py tests/test_overlay.py`

Next worthwhile areas
- `web/src/i18n/request.ts`: request-time locale loading still pulls the full locale payload and remains the next web render-path hotspot.
- `web/src/components/SiteHeader.tsx`: the mobile menu still bundles a large interactive path into the header shell and is the next client-side bundle/hydration candidate once it is safely outside the recent-change boundary.
- `ui/overlay.py`: if traces still show overlay cost during active speech, the next step is to reduce per-frame math/allocation work inside `_render_level_frame()` rather than the timer cadence itself.

Performance work log - 2026-04-09 (web request-config cache pass)

Scope
- Left the last two web commit areas untouched again: `web/src/app/[locale]/page.tsx`, `web/src/app/[locale]/docs/page.tsx`, `web/src/app/[locale]/layout.tsx`, `web/src/components/SiteHeader.tsx`, `web/src/components/LanguageSwitcher.tsx`, and related tests stayed out of scope.
- Focused on the next untouched web render-path hotspot from the notes backlog: `web/src/i18n/request.ts`.
- Preserved locale routing, translation lookup behavior, and existing page/build output.

Current weaknesses reviewed
- `web/src/i18n/request.ts`: the request config used a dynamic JSON import on every lookup path and rebuilt the `{locale, messages}` object even though the app only has two static locale payloads.
- `web/src/i18n/request.ts`: explicit locale overrides supported by `next-intl` were not consumed, so the config path still awaited `requestLocale` unnecessarily.
- `web/src/test/setup.ts`: the shared `next-intl/server` mock did not expose `getRequestConfig`, which blocked focused regression coverage for the request-config path.

Changes in this pass
- `web/src/i18n/request.ts`
  Replaced the dynamic per-request locale import with a static in-memory locale map and cached request-config objects for `en` and `de`.
- `web/src/i18n/request.ts`
  Honored the explicit `locale` override before awaiting `requestLocale`, so server-side callers can take the direct fast path.
- `web/src/i18n/request.test.ts`
  Added regression coverage for invalid-locale fallback, override-without-await behavior, and repeated-locale config reuse.
- `web/src/test/setup.ts`
  Extended the shared `next-intl/server` test mock with `getRequestConfig` so request-config tests run against the existing Web test harness cleanly.

Why this helps
- Request-time i18n setup no longer pays dynamic import/module-resolution work on the hot path for the only two supported locales.
- Reusing stable per-locale config objects trims repeated allocation work and keeps server render-path setup more predictable.
- Explicit locale overrides now avoid an unnecessary async wait, which slightly shortens metadata/request helper paths that already know their locale.

Validation
- `cd web && npm run test:run -- src/i18n/request.test.ts src/components/LanguageSwitcher.test.tsx src/components/SiteHeader.test.tsx`
- `cd web && npm run lint -- src/i18n/request.ts src/i18n/request.test.ts src/test/setup.ts`
- `cd web && npm run build`

Next worthwhile areas
- `web/src/components/SiteHeader.tsx`: once it is outside the recent-commit boundary, split the always-visible shell from the mobile-menu interaction path to reduce hydrated client work.
- `web/src/app/[locale]/layout.tsx`: if traces still show client payload overhead, pass an explicit minimal or null `messages` prop to `NextIntlClientProvider` instead of letting it resolve the full request message set implicitly.
- `web/src/i18n/request.ts`: if server traces still show i18n cost after this cache pass, the next step is namespace-level message loading rather than more micro-optimizations in page bodies.

Performance work log - 2026-04-09 (Windows settings prompt save pass)

Scope
- Left the last two root commits untouched: `notes.txt` and the recent `ui/overlay.py` timer work stayed out of scope.
- Focused on the next untouched Windows frontend save-path hotspot in `ui/settings_windows.py`.
- Preserved prompt editing behavior, prompt-context switching, and `Save && Apply` semantics.

Current weaknesses reviewed
- `ui/settings_windows.py`: visiting the Prompts tab populated `_prompts_cache` with loaded prompt text even when the user made no edits.
- `ui/settings_windows.py`: the general `Save && Apply` path always ran `_save_all_prompts()` against that cache, so unchanged prompt tabs still triggered prompt loading, merge work, serialization, and a `prompts.toml` rewrite attempt.
- `ui/settings_windows.py`: prompt context switches cached text but did not distinguish between loaded-baseline data and actual user edits, so unchanged prompt state looked dirty to the save path.

Changes in this pass
- `ui/settings_windows.py`
  Added prompt-session dirty tracking plus a cached loaded-prompt snapshot so unchanged prompt contexts no longer count as pending writes.
- `ui/settings_windows.py`
  Routed prompt context caching through baseline comparison, which keeps viewed-but-unchanged contexts clean while still marking real edits and resets as dirty.
- `ui/settings_windows.py`
  Updated `_save_all_prompts()` and `_save_current_prompt()` to skip no-op prompt persistence work and only merge/save truly dirty contexts.
- `tests/test_settings_windows_prompts.py`
  Added regression coverage that unchanged prompt views do not trigger prompt rewrites and that dirty-context saves keep existing overrides intact.

Why this helps
- The main Windows `Save && Apply` path is cheaper after prompt-tab visits because it avoids unnecessary prompt TOML parsing, merge work, and rewrite attempts when the prompt data is effectively unchanged.
- Prompt context switching stays responsive without turning every loaded context into extra save-time work later in the session.
- Dirty-only prompt persistence keeps the architecture straightforward: cache loaded state once, mark real edits, and persist only the delta.

Validation
- `pytest tests/test_settings_windows_prompts.py tests/test_settings_windows_load.py -q`
- `python -m py_compile ui/settings_windows.py tests/test_settings_windows_prompts.py tests/test_settings_windows_load.py`
- `ruff check ui/settings_windows.py tests/test_settings_windows_prompts.py tests/test_settings_windows_load.py`

Next worthwhile areas
- `ui/settings_windows.py`: the large `_load_settings()` field population path is still the next Windows UI hotspot if the settings dialog is reopened or rebuilt often.
- `ui/settings_windows.py`: the logs/transcripts tab can still be profiled for additional status-label and view-refresh deduping during long-running sessions.
- `web/src/components/SiteHeader.tsx`: on the web side, the mobile-menu interaction path is still the clearest remaining hydrated client-work candidate once it is safely outside the recent-change boundary.

Performance work log - 2026-04-09 (Windows onboarding fast-path follow-up)

Scope
- Left the last two commit areas untouched: `ui/welcome.py`, `utils/history.py`, `ui/settings_windows.py`, and their latest tests stayed out of scope.
- Focused on one remaining Windows onboarding save/navigation hotspot in `ui/onboarding_wizard_windows.py`.
- Preserved onboarding flow, Fast-mode behavior, summary/test labels, and hotkey handling.

Current weaknesses reviewed
- `ui/onboarding_wizard_windows.py`: the Fast-mode path still split Deepgram key persistence and mode selection across separate save logic, which added avoidable `.env` write/setup work in one UI action.
- `ui/onboarding_wizard_windows.py`: step navigation still refreshed the wizard env cache on every step transition even though the dialog already maintains the current state for its own writes.
- `ui/onboarding_wizard_windows.py`: test-hotkey and summary labels rebuilt the same hotkey presentation separately, adding a small but repeated render-adjacent code path.

Changes in this pass
- `ui/onboarding_wizard_windows.py`
  Batched Fast-mode Deepgram key persistence and `PULSESCRIBE_MODE` updates into the wizard-local env update path and skipped the batch entirely for duplicate values.
- `ui/onboarding_wizard_windows.py`
  Stopped unconditional env-cache refreshes during `_show_step()`, relying on the wizard's maintained cache except where external preset helpers already trigger an explicit refresh.
- `ui/onboarding_wizard_windows.py`
  Consolidated hotkey summary formatting so test and cheat-sheet labels share one helper.
- `tests/test_onboarding_windows.py`
  Added regression coverage for the batched Fast-mode update and for step transitions not forcing an env-cache refresh.

Why this helps
- Advancing through Fast-mode performs one coherent persistence pass instead of separate key/mode work, reducing disk churn on a user-visible onboarding action.
- Step changes no longer pay repeated env reload work when the wizard already knows the current values, which keeps navigation lighter.
- Shared hotkey summary formatting trims duplicate render-adjacent work and keeps the display path simpler.

Validation
- `pytest tests/test_onboarding_windows.py -q`
- `python -m py_compile ui/onboarding_wizard_windows.py tests/test_onboarding_windows.py`
- `ruff check ui/onboarding_wizard_windows.py tests/test_onboarding_windows.py`

Next worthwhile areas
- `ui/settings_windows.py`: `_load_settings()` remains the clearest Windows UI hot path if the settings dialog is reopened frequently.
- `ui/overlay.py`: if macOS traces still show overlay cost during active speech, reduce per-frame math/allocation work in `_render_level_frame()`.
- `utils/ipc.py`: the Windows onboarding test flow may still benefit from reducing redundant file/status reads now that the UI-side cadence has already been tuned.

Performance work log - 2026-04-09 (Windows polling and tail helper pass)

Scope
- Left the last two root commits untouched: `ui/onboarding_wizard_windows.py`, `ui/welcome.py`, and `utils/history.py` stayed out of scope in this pass.
- Avoided unrelated local worktree edits in `utils/custom_prompts.py` and `tests/test_custom_prompts.py`.
- Focused on shared polling/file-tail helpers used by the Windows overlay and logs paths.

Current weaknesses reviewed
- `utils/log_tail.py`: multiple tail/signature helpers performed separate `exists()`/`stat()`/open sequences, which adds avoidable filesystem work on polling-heavy UI paths.
- `ui/overlay_windows.py`: interim polling checked file existence, then stat metadata, then reopened the file, so each recording poll paid duplicate file metadata work before any real text change handling.
- `ui/settings_windows.py`: log and transcript refreshes still depend on the shared tail/signature helpers, so duplicate filesystem metadata work there propagates directly into the visible settings UI.

Changes in this pass
- `utils/log_tail.py`
  Consolidated tail-reader setup around one open/size path and removed redundant pre-check syscalls from signature and append-read helpers.
- `ui/overlay_windows.py`
  Switched interim-file change detection to the shared `(mtime_ns, size)` signature and reset/compare that cached signature instead of doing separate existence and mtime checks.
- Tests
  Added regression coverage for the overlay signature cache reset path and kept the shared log-tail tests green against the new helper behavior.

Why this helps
- High-frequency overlay polling now does less repeated filesystem metadata work while recording.
- Shared tail helpers are cheaper for every caller that only needs "has this file changed?" or "read the tail from here?" behavior.
- The Windows logs/transcript views benefit indirectly because their incremental refresh paths now lean on cheaper shared file-tail primitives.

Validation
- `pytest tests/test_log_tail.py tests/test_overlay_windows.py tests/test_settings_windows_logs.py -q`
- `python -m py_compile utils/log_tail.py ui/overlay_windows.py ui/settings_windows.py tests/test_log_tail.py tests/test_overlay_windows.py tests/test_settings_windows_logs.py`
- `ruff check utils/log_tail.py ui/overlay_windows.py ui/settings_windows.py tests/test_log_tail.py tests/test_overlay_windows.py tests/test_settings_windows_logs.py`

Next worthwhile areas
- `ui/settings_windows.py`: `_load_settings()` still repopulates many fields in one pass and remains the clearest remaining Windows settings hotspot.
- `ui/overlay.py`: the macOS overlay still does per-frame math for every active wave bar, which is the next likely place to trim render cost on long recordings.
- `utils/ipc.py`: if trace data still shows polling overhead during Windows setup/recording transitions, reduce repeated file-status reads there next.

Performance work log - 2026-04-09 (Windows logs auto-refresh cadence pass)

Scope
- Left the last two root commits untouched: `ui/onboarding_wizard_windows.py`, `ui/welcome.py`, and `utils/history.py` stayed out of scope again.
- Avoided unrelated local worktree edits in `utils/custom_prompts.py` and `tests/test_custom_prompts.py`.
- Focused on the remaining Windows settings hotspot called out in the notes backlog: idle wakeups and no-op polling pressure in the Logs/Transcripts tab.

Current weaknesses reviewed
- `ui/settings_windows.py`: the Logs tab auto-refresh stayed at a fixed 2-second cadence even after several unchanged polls, so an open settings window kept waking the UI and touching the filesystem more often than necessary.
- `ui/settings_windows.py`: after the cadence had been slowed by stable content, explicit user interactions like opening the Logs tab, switching segments, or pressing Refresh did not restore the fast cadence immediately.

Changes in this pass
- `ui/settings_windows.py`
  Added a small adaptive auto-refresh cadence for the Logs/Transcripts tab that backs off from 2s to 4s to 8s while the visible content remains unchanged.
- `ui/settings_windows.py`
  Reset the cadence to the fast path on user-driven interactions such as opening the Logs tab, switching between Logs and Transcripts, enabling auto-refresh, and manual refresh actions.
- `ui/settings_windows.py`
  Kept the existing no-op guard for reselecting the already active Logs/Transcripts segment so the new cadence logic does not reintroduce redundant refresh work.
- Tests
  Added regression coverage for idle backoff and fast-cadence reset.

Why this helps
- A long-lived Windows settings session now does less timer and file-tail work when the log/transcript files are stable, which reduces idle CPU churn in a visible troubleshooting path.
- As soon as the user looks at the tab again or content starts changing, the refresh cadence snaps back to the more responsive interval instead of feeling stale.
- The change stays local to the Logs tab and does not alter log/transcript rendering semantics.

Validation
- `pytest tests/test_settings_windows_logs.py tests/test_settings_windows_load.py -q`
- `python -m py_compile ui/settings_windows.py tests/test_settings_windows_logs.py`
- `ruff check ui/settings_windows.py tests/test_settings_windows_logs.py`

Next worthwhile areas
- `ui/settings_windows.py`: `_load_settings()` is still the clearest remaining Windows settings hotspot if dialog-open traces keep showing bulk widget churn.
- `ui/overlay.py`: the macOS overlay still looks like the next long-session render hotspot if active recording traces remain expensive.
- `utils/ipc.py`: once the UI-side cadence has been tuned, the next Windows onboarding/test-step win is likely reducing repeated IPC file reads themselves.
