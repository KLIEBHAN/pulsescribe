UX notes - 2026-04-19

Latest pass - Overlay state UX (`ui/overlay.py`, `ui/overlay_pyside6.py`, `ui/overlay_windows.py`)
- Scope: improved the remaining broad-impact status views outside Onboarding / Settings / Welcome / Menubar.
- Reviewed flows:
  - macOS overlay
  - Windows PySide6 overlay
  - Windows Tk fallback overlay
- Main weaknesses found:
  1. Several transient states still used overly generic copy (`Done!`, `Error`, `Loading model...`).
  2. Success / failure overlays did not communicate the outcome clearly enough in short-lived states.
  3. The PySide6 overlay had room for better accessibility metadata on its main status widgets.
- Changes in this pass:
  - added dedicated overlay feedback defaults with clearer user-facing outcome text
  - improved transient copy such as `Preparing transcription…`, `Transcript pasted`, and better processing wording
  - kept explicit error details when available while improving generic fallback error text
  - added basic accessible names / descriptions to the PySide6 overlay and kept the live label description in sync with visible text
  - kept behavior / timing / state flow unchanged
- Validation:
  - `pytest tests/test_overlay.py tests/test_overlay_pyside6.py tests/test_overlay_windows.py tests/test_daemon_mode.py tests/test_windows_race_conditions.py -q`
  - `python -m py_compile ui/overlay_feedback.py ui/overlay.py ui/overlay_pyside6.py ui/overlay_windows.py tests/test_overlay.py tests/test_overlay_pyside6.py tests/test_overlay_windows.py`
  - `ruff check ui/overlay_feedback.py ui/overlay.py ui/overlay_pyside6.py ui/overlay_windows.py tests/test_overlay.py tests/test_overlay_pyside6.py tests/test_overlay_windows.py`

Previous pass - Shared action/status card UX (`ui/hotkey_card.py`, `ui/permissions_card.py`)
- Improved friendlier hotkey formatting, clearer recording/empty states, better permission microcopy, and added a card-level permission summary.

Previous pass - Menubar action flow UX (`ui/menubar.py`)
- Added clearer status/hint rows, better transient menubar state text, clearer action labels, and explicit feedback for log/diagnostics actions.

Previous pass - Welcome + status feedback UX (`ui/welcome.py`, overlays)
- Improved Welcome provider guidance, Try-it empty states, footer save feedback, and unified overlay status/error messaging.

Previous pass - Settings UX (`ui/settings_windows.py`)
- Improved provider guidance, inline API-key states, Setup overview copy, Settings hotkey guidance, and persistent footer save feedback.

Previous pass - Onboarding / shared setup UX
- Improved Windows onboarding guidance, hotkey capture discoverability, safe dictation test messaging, shared permission card wording, and Windows focus/placeholder styling.

Next worthwhile UX areas
1. welcome logs / transcripts panel
- clearer refresh / empty / failure feedback and stronger visual hierarchy between logs vs transcript history
2. advanced local settings copy
- explain defaults vs overrides and performance tradeoffs even more clearly
3. transient daemon / retry states outside the overlay
- clearer recovery messaging when startup / provider initialization fails
