Performance work log - 2026-03-28

Guardrails
- Focused on performance-heavy UI paths outside the last two commits unless compatibility required otherwise.
- Left the recent work in `ui/welcome.py`, `ui/settings_windows.py`, `ui/onboarding_wizard.py`, `ui/onboarding_wizard_windows.py` and `ui/hotkey_card.py` alone for this pass.
- Kept onboarding flows, hotkeys, permissions behavior and overlay UX functionally unchanged.

Recent passes kept
- `ui/welcome.py`: lazy-built heavy welcome/settings tabs and deferred the transcripts panel/history load until the user actually opens it.
- `ui/settings_windows.py`: lazy-built `Prompts`, `Vocabulary` and `Logs`; skipped no-op editor rewrites; cached the vocabulary file signature to avoid redundant reloads.
- `ui/onboarding_wizard.py` / `ui/onboarding_wizard_windows.py` / `ui/hotkey_card.py`: deduped repeated label, button, preview, transcript and status updates to trim render churn during onboarding and hotkey capture.
- `ui/overlay.py` / `ui/overlay_windows.py` / `ui/overlay_pyside6.py`: cached duplicate overlay text/color/geometry work and avoided redundant redraws or polling where possible.
- `ui/permissions_card.py` / `ui/menubar.py`: skipped unchanged permission widget mutations, ended permission polling early once complete, and avoided duplicate menu bar title writes.

Current pass: macOS overlay timer throttling
Hotspot reviewed
- `ui/overlay.py`: listening, transcribing/refining and success feedback animations still scheduled timer-driven frames at 60 FPS even though those states do not need live audio-level responsiveness.

Changes
- `ui/overlay.py`: kept recording at 60 FPS, but reduced timer-driven waiting/processing animation loops to 30 FPS and done-feedback frames to 20 FPS.
- `tests/test_overlay.py`: added regression coverage to assert the actual scheduled timer intervals for recording, active and feedback animation timers.

Why this helps
- Lowers steady-state CPU wakeups and Core Animation work while the overlay is visible in non-recording states.
- Preserves recording responsiveness because live audio visualization still uses the existing 60 FPS path.

Validation
- `pytest tests/test_overlay.py tests/test_overlay_windows.py tests/test_overlay_pyside6.py tests/test_animation.py -q`
- `python -m py_compile ui/overlay.py tests/test_overlay.py`

Next worthwhile areas
- `ui/settings_windows.py`: logs and transcripts still replace the full viewer payload when content changes; incremental append/chunked updates remain the next large-file win.
- `ui/welcome.py`: the logs view still swaps the full tailed text buffer on each file change; append-style updates would further reduce redraw cost on busy sessions.
- `ui/onboarding_wizard_windows.py`: choice buttons still toggle all checked states on each selection if that step becomes hot again.
- `ui/overlay.py`: if runtime profiling still shows recording-path cost, silence-aware throttling for bar updates would be the next targeted optimization.
