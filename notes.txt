Performance work log - 2026-03-28

Guardrails
- Focused on UI-heavy paths outside the last two commits.
- Left the recent work in `ui/welcome.py`, `ui/onboarding_wizard.py`, `ui/onboarding_wizard_windows.py`, `ui/overlay_pyside6.py` and `ui/overlay_windows.py` alone unless compatibility required it.
- Kept onboarding flows, hotkeys, permissions behavior and overlay UX functionally unchanged.

Previously completed
- `ui/welcome.py`: lazy-built heavy welcome/settings tabs.
- `ui/overlay_windows.py`: skipped duplicate label and bar-color updates in the Windows Tk overlay.
- `ui/onboarding_wizard.py` / `ui/onboarding_wizard_windows.py`: lazy-built onboarding steps/pages.
- `ui/overlay_pyside6.py`: cached duplicate label font/style/text work in the Windows/PySide overlay.

Earlier pass: hotspots reviewed
- `ui/overlay.py`: the macOS overlay still recreated text styling on state churn and pushed CALayer geometry updates every frame, including redundant position writes.
- `ui/permissions_card.py`: short-lived permission polling kept reapplying identical field/button state and continued polling even after all permissions were already granted.
- `ui/menubar.py`: recording updates rewrote the menu bar title even when the visible preview string had not changed.

Earlier pass: changes
- `ui/overlay.py`: cached overlay text font/color/value state so repeated recording/listening/transcribing updates skip duplicate `setFont_`, `setTextColor_` and `setStringValue_` calls.
- `ui/overlay.py`: cached static bar positions and skipped sub-pixel CALayer height writes to reduce geometry churn in the macOS animation path.
- `ui/permissions_card.py`: cached status/button presentation per row so repeated refreshes avoid redundant widget mutations.
- `ui/permissions_card.py`: stopped auto-refresh early once microphone, accessibility and input-monitoring permissions are all granted.
- `ui/menubar.py`: cached the last visible menu bar title and skipped duplicate title writes, including truncated recording previews that resolve to the same visible text.
- Added regression tests for the new overlay no-op guards, menu bar deduping and permission-card refresh/polling behavior.

Earlier pass: why this helps
- Lower steady-state macOS overlay cost during recording and animated states because the hot path now avoids repeated text styling work and unnecessary layer mutations.
- Lower onboarding/settings permission overhead because the card stops mutating unchanged controls and quits polling as soon as there is nothing left to wait for.
- Lower menu bar churn during recording because identical visible titles no longer trigger redundant AppKit updates.

Earlier pass: validation
- `pytest tests/test_overlay.py tests/test_menubar.py tests/test_permissions_card.py tests/test_onboarding.py tests/test_onboarding_wizard_flow.py -q`

Latest pass: hotspots reviewed
- `ui/settings_windows.py`: the Windows settings window still eagerly built the heavy `Prompts`, `Vocabulary` and `Logs` tabs even before the user visited them.
- `ui/settings_windows.py`: vocabulary reloads still re-read/re-validated the same file on every revisit and rewrote large editor contents even when nothing changed.
- `ui/welcome.py`: the macOS Logs tab still created the hidden transcripts panel and loaded transcript history immediately, even when the user only needed logs.

Latest pass: changes
- `ui/settings_windows.py`: lazy-built `Prompts`, `Vocabulary` and `Logs` so the settings window no longer pays those text-editor construction costs on open.
- `ui/settings_windows.py`: added no-op `QPlainTextEdit` guards for prompt and vocabulary reloads so unchanged text no longer rebuilds the document.
- `ui/settings_windows.py`: cached the vocabulary file signature and skipped repeated auto-reloads/validation while the underlying file is unchanged; the explicit `Load` action still forces a refresh.
- `ui/welcome.py`: deferred transcripts controls and the first transcript-history load until the user actually switches from Logs to Transcripts.
- Added regression tests for Windows lazy-tab initialization, vocabulary signature guards and the macOS on-demand transcripts build.

Latest pass: why this helps
- Faster perceived open time for the Windows settings window because the common path no longer constructs the heavy text tabs up front.
- Lower repeated UI churn in prompt/vocabulary editors because identical content no longer triggers full `QPlainTextEdit` document resets.
- Faster first entry into the macOS Logs tab because the hidden transcripts view is no longer built or populated until needed.

Latest pass: validation
- `pytest tests/test_settings_windows_load.py tests/test_settings_windows_logs.py tests/test_settings_windows_prompts.py tests/test_settings_ui.py -q`

Next worthwhile areas
- `ui/settings_windows.py`: logs and transcripts still replace the full viewer payload when content changes; incremental append/chunked updates are the next targeted win for very large histories.
- `ui/welcome.py`: the logs view still swaps the full tailed text buffer on each file change; append-style updates would further reduce redraw cost on busy sessions.
- `ui/overlay.py`: if runtime profiling still shows overlay cost, state-based FPS reduction for non-recording animations is the next targeted optimization.
