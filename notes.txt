Performance work log - 2026-03-28

Scope
- Left the last two commit areas untouched: transcript/log incremental refresh and permission/overlay repaint work.
- Focused on `.env` persistence churn in Windows settings/local preset flows and setup overview UI churn.
- Preserved current onboarding, settings, preset, and API key behavior.

Current weaknesses reviewed
- `ui/settings_windows.py`: `Save && Apply` rewrote `.env` once per setting and once per API key, causing a long chain of small disk reads/writes in one UI action.
- `utils/presets.py`: applying a local preset performed many separate `.env` mutations even though the user triggered one atomic configuration change.
- `ui/settings_windows.py`: setup overview labels/styles and mode-dependent card visibility were reapplied even when the rendered result had not changed, causing avoidable repaints and layout work.

Changes in this pass
- `utils/preferences.py`
  Added `update_env_settings()` to batch multiple `.env` mutations into one atomic read/write pass, preserve unrelated lines, collapse duplicate updated keys, and skip unchanged rewrites.
- `ui/settings_windows.py`
  Switched the Windows settings save path to one batched `.env` update for settings and API keys, and deduped repeated setup overview/status/visibility widget updates.
- `utils/presets.py`
  Switched local preset application to the batched `.env` update path.
- Tests
  Added regression coverage for batched env writes, no-op env updates, preset batching, canonical FP16 handling in batched saves, and batched API key persistence.

Why this helps
- `Save && Apply` no longer blocks on a long series of tiny `.env` writes, so the settings window responds faster and feels less sticky.
- Local preset application now lands as one atomic config update instead of many serial writes.
- Setup overview refreshes and mode switches avoid redundant text/style/visibility churn, which trims repaint/layout pressure in a frequently hit UI path.

Validation
- `pytest tests/test_preferences_cache.py tests/test_presets.py tests/test_settings_windows_load.py tests/test_settings_windows_hotkeys.py tests/test_settings_windows_logs.py tests/test_onboarding_windows.py -q`
- `python -m py_compile utils/preferences.py utils/presets.py ui/settings_windows.py tests/test_preferences_cache.py tests/test_presets.py tests/test_settings_windows_load.py`
- `ruff check utils/preferences.py utils/presets.py ui/settings_windows.py tests/test_preferences_cache.py tests/test_presets.py tests/test_settings_windows_load.py`

Next worthwhile areas
- `ui/onboarding_wizard_windows.py`: hotkey/test-summary paths still reread env-backed values more often than needed; a small local state cache would trim more UI churn.
- `ui/welcome.py`: the save/apply path still performs many individual env writes, but it was intentionally skipped here because welcome/log work was part of the recent commit history.
- `utils/custom_prompts.py` and vocabulary-driven tab loads: if profiling still shows tab-open spikes, add signature-based caches similar to the history/log optimizations.

Performance work log - 2026-03-28 (Onboarding pass)

Scope
- Kept the last two commit areas untouched again: settings/env batching setup work and permission/overlay repaint work.
- Focused on `ui/onboarding_wizard_windows.py`, which was the clearest remaining frontend hotspot outside those commit boundaries.
- Preserved onboarding flow, hotkey behavior, summary output, and daemon settings reload semantics.

Current weaknesses reviewed
- `ui/onboarding_wizard_windows.py`: the wizard repeatedly reread env-backed values across choose-goal, hotkey, test, summary, and navigation paths even though the same values were already known within the current dialog session.
- `ui/onboarding_wizard_windows.py`: fast/private onboarding could trigger redundant persistence work, including duplicate Deepgram key handling on Next and an extra `PULSESCRIBE_MODE=local` write before local preset application.
- `ui/onboarding_wizard_windows.py`: hotkey defaults, hotkey saves, preset application, and clears performed separate per-key env mutations and always emitted `settings_changed`, even when nothing actually changed.

Changes in this pass
- `ui/onboarding_wizard_windows.py`
  Added a lightweight wizard-local env cache with explicit refresh on step changes, so hotkey/mode/language/API-key reads no longer keep walking the env helper path on every small UI update.
- `ui/onboarding_wizard_windows.py`
  Added a single batched env update helper for the wizard and routed hotkey defaults, hotkey edits, preset application, language changes, and fast-mode mode selection through it.
- `ui/onboarding_wizard_windows.py`
  Removed duplicate fast/private writes: Next now relies on the preset application path for Deepgram key persistence, and private mode no longer writes `PULSESCRIBE_MODE=local` twice before the local preset helper runs.
- `ui/onboarding_wizard_windows.py`
  Suppressed no-op `settings_changed` emissions when language, hotkeys, API key, or mode were already current, which avoids unnecessary daemon reload work from the onboarding UI.
- Tests
  Updated Windows onboarding tests for the batched persistence path and added coverage for no-op language changes and duplicate fast-mode persistence avoidance.

Why this helps
- The Windows onboarding dialog now does materially less env I/O while the user configures hotkeys, mode, and language.
- Hotkey interactions persist as one atomic pair update instead of multiple independent writes, which shortens the critical UI path and keeps the state transition consistent.
- Avoiding no-op `settings_changed` emissions removes unnecessary downstream reload work, improving responsiveness especially when users revisit the same selection or preset.

Validation
- `pytest tests/test_onboarding_windows.py -q`
- `python -m py_compile ui/onboarding_wizard_windows.py tests/test_onboarding_windows.py`
- `ruff check ui/onboarding_wizard_windows.py tests/test_onboarding_windows.py`

Next worthwhile areas
- `ui/onboarding_wizard.py` and `ui/hotkey_card.py`: the macOS onboarding flow still re-queries env-backed hotkey state during render cycles; a similar local-state pass would keep cross-platform behavior aligned.
- `ui/welcome.py`: the welcome save/apply flow still has a large batched-settings opportunity, but it remains adjacent to recent commit history and should be handled in a separate pass.
- Prompt/vocabulary editor tabs remain the next likely source of open-tab latency if profiling still shows spikes after the onboarding cleanup.

Performance work log - 2026-04-09 (macOS welcome + onboarding pass)

Scope
- Kept the last two commits untouched again: Windows settings batching and Windows onboarding env churn.
- Focused on the next macOS/frontend hotspots instead: `ui/welcome.py` and `ui/onboarding_wizard.py`.
- Preserved onboarding flow, welcome-window behavior, prompt/vocabulary editing, and settings persistence semantics.

Current weaknesses reviewed
- `ui/welcome.py`: `Save & Apply` still wrote `.env` one key at a time, so one UI action could trigger a long serial chain of reads/writes.
- `ui/welcome.py`: prompt/vocabulary tabs reloaded backing data more often than needed during a single window session.
- `ui/welcome.py`: mode-dependent hide/show updates still hit AppKit views even when the visibility state was unchanged.
- `ui/onboarding_wizard.py`: render-adjacent paths (`_can_advance`, summary, hotkey preview, fast-mode selection) repeatedly reread env-backed values inside the same wizard session.

Changes in this pass
- `ui/welcome.py`
  Switched the macOS `Save & Apply` path to a single batched `.env` update, including API keys, provider settings, local overrides, refine/display toggles, and legacy key cleanup.
- `ui/welcome.py`
  Added lightweight session caches for loaded prompts/defaults and vocabulary keywords so tab-open and save paths stop reloading the same data repeatedly within one window session.
- `ui/welcome.py`
  Deduped mode-dependent visibility changes so AppKit hide/show work is skipped when the effective state is already current.
- `ui/onboarding_wizard.py`
  Added a wizard-local env cache and routed fast-mode persistence, language persistence, hotkey validation/save state, summary rendering, and test-hotkey preview reads through it.
- Tests
  Updated welcome/onboarding tests for batched env persistence, session caching, and no-op visibility churn suppression.

Why this helps
- The macOS welcome window now persists settings in one atomic `.env` write instead of many small writes, which shortens the critical save path and reduces UI stickiness.
- Prompt/vocabulary tabs reuse already loaded data during the same session, so opening or saving those heavier editors avoids redundant disk work and recomputation.
- The macOS onboarding wizard no longer re-reads the same env values on each small render/update step, which trims unnecessary work in a render-sensitive first-run flow.
- Skipping redundant AppKit `setHidden_` churn reduces needless UI work on frequent mode/backend toggles.

Validation
- `pytest tests/test_settings_ui.py tests/test_onboarding.py tests/test_onboarding_wizard_flow.py -q`
- `pytest tests/test_custom_prompts.py tests/test_vocabulary.py -q`
- `python -m py_compile ui/welcome.py ui/onboarding_wizard.py tests/test_settings_ui.py tests/test_onboarding.py tests/test_onboarding_wizard_flow.py`
- `ruff check ui/welcome.py ui/onboarding_wizard.py tests/test_settings_ui.py tests/test_onboarding.py tests/test_onboarding_wizard_flow.py`

Next worthwhile areas
- `ui/hotkey_card.py`: still reads env-backed hotkey state directly; a tiny shared cache layer would align it with the wizard optimizations.
- `ui/welcome.py` logs/transcripts view switching could be profiled for further paint/layout trimming if the window is kept open for long sessions.
- If tab-open latency remains noticeable, the next likely area is moving prompt/vocabulary validation work off the immediate interaction path.

Performance work log - 2026-04-09 (hotkey card follow-up)

Scope
- Built on the current macOS welcome/onboarding state without touching the last two Windows-focused commits.
- Focused only on the shared hotkey UI path in `ui/hotkey_card.py` and its macOS callers in `ui/welcome.py` and `ui/onboarding_wizard.py`.
- Preserved hotkey validation, persistence semantics, recorder behavior, and visible UI flow.

Current weaknesses reviewed
- `ui/hotkey_card.py`: after successful hotkey changes, the card still reread hotkeys through the env/preferences path even when the parent controller already had current values cached.
- `ui/welcome.py`: the welcome window cached env settings broadly, but hotkey saves did not feed that cache back into the shared hotkey card.
- `ui/onboarding_wizard.py`: the wizard already tracked cached hotkey state, but the shared hotkey card still fell back to fresh env reads instead of reusing it.

Changes in this pass
- `ui/hotkey_card.py`
  Added an optional current-hotkeys provider so the shared card can sync from caller-owned cached state instead of always reading env-backed preferences.
- `ui/welcome.py`
  Wired the hotkey card to the welcome-window env cache and refreshed that cache immediately after successful hotkey saves.
- `ui/onboarding_wizard.py`
  Wired the hotkey card to the wizard-local hotkey cache so repeat syncs during onboarding stay in-memory.
- Tests
  Added regression coverage that the shared hotkey card prefers the cached provider path over direct env reads.

Why this helps
- Hotkey preset clicks, recorded hotkey saves, and follow-up field syncs now avoid redundant env lookups in the main macOS UI flows.
- The shared hotkey component no longer forces an extra read path immediately after the controller has already updated its own hotkey state.
- This trims repeated work on a user-visible interaction path that tends to be exercised several times while configuring shortcuts.

Validation
- `pytest tests/test_hotkey_card.py tests/test_settings_ui.py tests/test_onboarding.py tests/test_onboarding_wizard_flow.py -q`
- `python -m py_compile ui/hotkey_card.py ui/welcome.py ui/onboarding_wizard.py tests/test_hotkey_card.py tests/test_settings_ui.py`
- `ruff check ui/hotkey_card.py ui/welcome.py ui/onboarding_wizard.py tests/test_hotkey_card.py tests/test_settings_ui.py`

Next worthwhile areas
- `ui/welcome.py` logs/transcripts view switching remains the next likely long-session UI hotspot.
- `ui/onboarding_wizard.py` still has some render-adjacent status and summary recomputation that could be deduped further if profiling still shows churn.
- If prompt/vocabulary tabs still feel heavy, move non-essential validation or formatting work off the immediate interaction path.
