Performance work log - 2026-03-28

Scope
- Focused on frontend-heavy UI paths outside the last two commits.
- Left the recent settings-cache/prompt-load work and `audio/recording.py` untouched.

Hotspots reviewed
- `ui/welcome.py`: the macOS welcome window eagerly built every tab on open, including Prompts, Vocabulary, Logs, Transcripts and About.
- `ui/welcome.py`: the logs auto-refresh timer could start even before the Logs tab UI existed.
- `ui/overlay_windows.py`: the Windows overlay re-applied identical label configs and bar fill colors every frame.

Changes in this pass
- Added lazy tab building for the macOS welcome window so only the selected tab is built immediately; other tabs are created on first selection.
- Kept setup presets safe by materializing the Providers and Advanced tabs before a preset mutates hidden controls.
- Guarded logs auto-refresh so the timer stays off until the Logs tab has actually been built.
- Cached Windows overlay label configs to skip duplicate `Label.config(...)` calls.
- Cached Windows overlay bar colors so identical `Canvas.itemconfig(...)` fill updates are not repeated every frame.
- Added regression tests for lazy-tab behavior, preset safety and the new overlay no-op guards.

Why this helps
- Faster welcome-window open time because heavy editors and log/history readers no longer initialize up front.
- Less background UI work before the user ever visits Logs.
- Lower steady-state Tkinter overhead during recording/feedback because repeated style writes are eliminated.

Validation
- `pytest tests/test_settings_ui.py tests/test_overlay_windows.py tests/test_overlay.py -q`

Next worthwhile areas
- macOS overlay rendering still updates every bar every frame and could be profiled for no-op geometry work.
- `ui/welcome.py` Logs tab still loads transcript history when the tab is built; segment-level lazy loading would trim that path further.
- Large onboarding windows (`ui/onboarding_wizard.py`, `ui/onboarding_wizard_windows.py`) are still good candidates for first-open profiling and deferred section construction.
